# Environment Configuration for Uru ChatGPT Interface
# Copy this file to .env for local development

# =============================================================================
# ESSENTIAL CONFIGURATION (Only things that change between deployments)
# =============================================================================

# Instance name - determines domain names for production
# Local: not used, Production: dynamosoftware.${INSTANCE}.com
INSTANCE=dev

# Security secret - MUST be different for each environment
SECRET_KEY=development_secret_key_change_in_production

# Database credentials (auto-generated by Elestio in production)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# =============================================================================
# OPTIONAL OVERRIDES (Usually not needed)
# =============================================================================

# Override API URL if needed (auto-configured based on INSTANCE)
# NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Override database URL if needed (auto-constructed from credentials)
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/uru_chatbot

# Override CORS origins if needed (auto-configured based on INSTANCE)
# CORS_ORIGINS=["http://localhost:3000"]

# Optional OpenAI API key (users typically provide their own)
# OPENAI_API_KEY=
